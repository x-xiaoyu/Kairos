# Kairos 产品说明与开发记录

> 文档快照时间：2026-09-15（America/New_York）  
> 记录范围：当前仓库中的 Kairos 产品目标、iPhone App 范围、已实现功能、关键交互规则、开发时间线、验证状态与后续工作。  
> 时间依据：Git 提交记录、源码文件修改时间、测试结果及本轮产品讨论。除可确认的时间外，不虚构精确完成时刻。

## 1. 产品概述

Kairos 是一个面向“知道要做什么，但难以决定现在先做什么”的个人执行与时间保护工具。

它不只保存待办事项，而是综合任务截止时间、预计时长、优先级、认知负荷和当天其他任务，回答三个核心问题：

1. 现在最应该做什么？
2. 最晚什么时候必须开始，才不容易错过截止时间？
3. 当计划发生变化时，今天剩余任务应该如何重新排序？

产品主张是：**Know when it’s time to start — 知道什么时候该开始。**

Kairos 的语气应当冷静、清楚、不评判。它的目标是帮助用户行动，而不是因为延期或未完成而制造负罪感。

## 2. 产品目标

### 2.1 核心目标

- 把模糊的待办事项变成可执行的时间计划。
- 根据任务的重要性、截止时间与耗时，给出当天的优先顺序。
- 计算每项任务的 Latest Safe Start（最晚安全开始时间）。
- 在计划开始时间或关键节点提醒用户开始行动。
- 提供清晰、远距离可读的专注倒计时。
- 允许真实生活中的并行活动，例如跑步时同时背单词。
- 记录用户当天做过什么，并可将完成的专注时段写入 Apple 日历。
- 通过本地规则、Kairos 后端 AI 或用户自己的 AI 帮助创建和调整任务。

### 2.2 设计目标

- 使用标准 Apple 系统字体，保证清晰度和原生感。
- 竖屏与横屏分别布局；横屏采用左右分区，而不是简单旋转或拉伸竖屏界面。
- 长标题必须被固定容器约束，最多显示两行，超出后使用省略号。
- 重点信息应当在远处也能看清，尤其是专注剩余时间。
- 关键提醒使用 Kairos 自己的全屏提醒页面，而不是普通的小型对话框。
- 首页、习惯、回顾、Kairos 与设置之间保持一致的页面导航体验。

## 3. 当前产品 Scope

### 3.1 已纳入范围

- 原生 iPhone SwiftUI App（主要个人使用入口）。
- SwiftData 本地任务、习惯和活动记录。
- 任务创建、编辑、删除与删除确认。
- 任务开始时间、截止时间、预计时长、优先级、认知负荷、是否可中断、截止类型。
- 首页主要倒数日。
- 当天任务排序与相邻顺延。
- Latest Safe Start 与风险等级。
- 本地通知、App 内全屏到点提醒和截止提醒。
- 普通专注、保持屏幕专注和多任务并行专注。
- Apple Calendar 专注记录。
- 习惯打卡与连续完成天数。
- 当日回顾时间线。
- 本地 Kairos Agent、后端 Kairos AI、用户自己的 OpenAI 连接。
- iPhone 主屏幕 Widget。
- iPhone 竖屏和横屏布局。
- 面向真机安装、签名和测试的开发流程。

### 3.2 当前不在范围内或受系统限制

- Kairos 不能阻止用户按 Home 键、锁屏或切换到其他 App。iOS 不允许普通 App 强制占据系统或控制其他 App。
- Kairos 不能在其他 App 上方展示自己的全屏页面。用户在其他 App 中时，只能依赖 iOS 通知；打开或返回 Kairos 后，才能展示 App 内全屏提醒。
- 普通本地通知的尺寸和从屏幕顶部出现的方式由 iOS 控制，App 无法自由改成任意大小。
- 真正的 Critical Alerts、系统级限制模式或更强的设备控制需要 Apple 的特殊 entitlement、Screen Time/Family Controls 资格或不同产品方案，目前不属于常规版本能力。
- 免费 Apple ID 适合连接真机进行个人测试，但安装签名通常有时间和设备方面的限制；大规模分发需要 Apple Developer Program/TestFlight 等正式方式。
- 当前 App 以本地、单设备体验为主，尚未把账户登录、云同步、多人协作作为核心范围。

## 4. 当前核心数据模型

### 4.1 任务 KairosTask

当前任务可保存：

- 标题与目标描述。
- 截止时间。
- 计划开始时间。
- 预计持续分钟数。
- 优先级。
- 状态：待办、进行中、已完成。
- 认知负荷：低、中、高。
- 是否可以被打断。
- 截止类型：硬截止、软截止、无截止。
- 是否作为首页主要倒数日。
- 创建时间、可开始时间和当天排序信息。

### 4.2 习惯 Routine

- 习惯标题。
- 每日目标分钟数。
- 当前连续完成天数。
- 最后完成日期。
- 每天只能完成一次；完成后显示“今日已完成”。

### 4.3 活动记录 ActivityEvent

- 操作类型。
- 对应任务或习惯名称。
- 说明文字。
- 时间戳。

用于“回顾今天”页面按发生顺序展示专注开始、暂停、结束、任务完成、习惯完成和日历保存结果。

## 5. 已实现功能说明

### 5.1 首页与计划

- 显示天气、日期、个人标题/目标、主要倒数日和当天任务列表。
- 首页右上角显示被标记为“首页主要倒数日”的任务。
- 倒数信息按时间量级显示，避免始终使用分钟：较长时间按小时或天表达。
- 计划器计算任务顺序、计划开始时间、结束时间和最晚安全开始时间。
- 风险状态包括 safe、warning、high 和 critical。
- 已经过期或临近截止的任务会成为首页关键行动提示。
- 计划卡片区分任务预计时长、计划开始时间和最晚安全开始时间。

### 5.2 新建与编辑任务

- 支持手动新建和编辑任务。
- 可设置计划开始时间，并在该时间安排提醒。
- 可设置截止日期和时间。
- 可将有截止时间的任务设为首页主要倒数日。
- 编辑页面左上角的返回承担取消职责，右上角保存，避免“返回”和“取消”重复。
- AI 一次提出多个任务时，以结构化列表展示，不再把全部内容挤成一个大段落。

### 5.3 任务删除

- 任务卡支持从右向左滑动显示删除操作。
- 静止状态不应提前露出红色删除背景。
- 删除前要求用户二次确认。
- 滑动和复位使用较柔和的动画，减少类似闪退的突兀感。

### 5.4 顺延规则

“Postpone”在产品中表示“顺延/推迟”。当前规则不是直接开始下一项，也不是把任务丢到当天最后：

- 仅处理与当前任务处于同一日程日期内的任务。
- 点击“顺延当前任务”时，只交换当前任务与紧邻的下一项任务。
- 下一项任务成为当前推荐任务。
- 原当前任务移动到下一项的位置。
- 不影响其他日期的任务，也不将所有任务整体后移。

### 5.5 到点提醒与截止提醒

- 任务可设置明确的计划开始时间。
- iOS 会在计划开始时间安排本地通知。
- App 位于前台或用户重新打开 App 时，会检查到点但尚未处理的任务。
- App 内使用独立的全屏提醒页面：大喇叭图形、放射线背景、任务名称、提醒原因和操作按钮。
- 用户可以立即开始专注，也可以关闭本次提醒。
- 长任务标题被限制在固定宽度/高度容器内，最多两行，超出使用省略号。
- 横屏提醒采用左侧信息、右侧操作的布局；竖屏采用上下布局。

### 5.6 专注模式

进入专注前先选择：

- **保持当前屏幕**：保持屏幕常亮；离开 Kairos 后暂停，并在返回时询问是否继续。
- **普通专注**：允许锁屏或切换其他 App，计时应继续。

普通专注不依赖 App 在后台每秒执行。实现方式是记录最近一次真实时间；用户返回前台时，根据系统时间差一次性扣除后台经过的秒数。这修复了此前“切出去后倒计时冻结”的问题。

当前专注页还支持：

- 大字号、等宽数字倒计时。
- 进度条。
- 暂停全部、继续全部、结束全部专注。
- 在专注过程中添加另一项待办任务。
- 多项任务同时按照各自的预计时长独立倒计时。
- 并行任务以多张专注卡片显示各自名称、时间和进度。
- 单独完成某项任务；该任务停止、记录完成并可写入日历，其余任务继续倒计时。
- 例如“跑步 60 分钟”和“背单词 20 分钟”可同时开始；20 分钟后先完成背单词，跑步继续。

### 5.7 Apple Calendar

- 设置中提供“保存完成的专注时段”开关。
- 用户完成任务时，Kairos 请求日历权限。
- 获得权限后，以该任务真实专注开始和结束时间创建日历事件。
- 每个并行任务独立保存自己的开始时间、结束时间和实际专注秒数。
- 没有日历权限或没有可写日历时，显示可理解的失败原因。

### 5.8 习惯

- 习惯作为当天打卡项目，而不是普通一次性任务。
- 未完成时提供完成今日习惯的操作。
- 完成后显示“今日已完成”。
- 显示连续完成天数，并使用成长/连续性视觉信息强化反馈。
- 打卡结果进入当日回顾。

### 5.9 回顾

- “回顾”作为独立导航页面，不使用底部弹窗。
- 展示当天完成数量、专注时长、专注事件数量。
- 按实际发生顺序展示活动时间线。

### 5.10 Kairos Agent

当前有三种模式：

- 本地：无需网络，使用设备内规则理解常见操作。
- Kairos AI：连接开发阶段的 Kairos 后端服务。
- 我的 AI：用户提供自己的 OpenAI API Key，Key 使用仅限本设备的 Keychain 存储。

Agent 可以提出但不直接静默执行数据修改，包括：

- 创建一项或多项任务。
- 顺延任务。
- 修改时长。
- 修改优先级。
- 完成任务。
- 重新规划。

涉及数据变化的建议需要用户确认。

### 5.11 Widget

- 小号 Widget 只突出主要倒数日，不显示多余 Logo 或装饰信息。
- 倒数日包含任务名称、距离目标的天数和对应截止日期。
- 长条 Widget 以类似 Apple Calendar 的方式突出下一项要做的任务。
- Widget 数据由主 App 写入共享 App Group，并在任务计划变化后刷新。

### 5.12 App Icon 与视觉

- 已配置 Kairos 的 iPhone App Icon 资源。
- 当前视觉语言以紫色、蓝色渐变和暖黄色强调色为主。
- 图标与页面设计强调时间节点、行动和开始，而不是复杂的 3D 装饰。

## 6. 开发时间线

### 2026-09-10：项目与 iPhone 基础能力

- 建立 Kairos 项目说明和基础仓库结构。
- 创建原生 SwiftUI App 入口与 SwiftData 模型容器。
- 建立任务规划、Latest Safe Start、风险等级和首页基础体验。
- 接入天气定位与 Kairos 视觉主题。
- 建立个人 AI Keychain 安全存储和连接页面。

### 2026-09-11：AI、真机与核心集成

- 完善 Kairos Agent 后端连接和个人 OpenAI 调用。
- 增加 Agent 响应解码与多操作建议测试。
- 处理 Xcode 真机选择、签名、开发者模式和连接问题。
- 修复项目中类型未加入编译目标所导致的一系列 scope/build 错误。
- 完成 Apple Calendar 专注记录的初步接入。

### 2026-09-12：任务、习惯、提醒、Widget 与导航

- 完善当天回顾页面和设置页面。
- 完善习惯打卡、今日完成状态和连续天数反馈。
- 增加主要倒数日字段与首页展示。
- 增加 Widget 数据快照及小号/长条 Widget。
- 完善任务创建和编辑：计划开始时间、截止时间、首页主要倒数日等。
- 将回顾、习惯、Kairos 和设置统一为页面导航体验。
- 完善当天任务排序和顺延逻辑：仅在当天内排序，只与下一项交换。
- 增加任务卡左滑删除、删除确认及滑动状态修正。
- 增加任务计划开始时间通知。
- 优化 AI 多任务创建建议的结构化显示。

### 2026-09-13：全屏提醒与品牌资源

- 将普通 App 内对话框替换为 Kairos 自己的全屏提醒页。
- 加入巨大喇叭、放射线背景、立即开始与关闭提醒操作。
- 对全屏提醒的长标题增加固定布局盒、两行限制和省略号。
- 分别调整竖屏和横屏布局，避免内容超出 iPhone 17 Pro Max 屏幕。
- 配置 Kairos App Icon 资源。

### 2026-09-15：后台计时与并行专注

- 修复普通专注切到后台后倒计时冻结的问题。
- 改用真实 wall-clock 时间差计算后台经过时间。
- 保留“保持当前屏幕”模式离开即暂停的独立规则。
- 增加在专注页选择并添加其他待办任务的入口。
- 增加多个任务并行倒计时界面。
- 支持每项任务独立完成，其他专注任务继续。
- 调整完成回调，使活动日志和 Apple Calendar 均使用实际完成的那一项任务。
- 新增后台时间扣减与不低于零的自动化测试。
- 在 iPhone 17 Pro Max 模拟器完成全量编译和测试。

## 7. 当前关键交互规则

### 7.1 时间显示

- 短时间使用分钟。
- 超过约一小时后优先使用小时表达。
- 超过 24 小时后使用天数表达。
- 倒数日同时展示目标任务与明确截止日期。

### 7.2 计划时间术语

- **预计时长**：完成任务预计需要多少分钟。
- **计划开始时间**：Kairos 安排或用户指定从几点开始。
- **最晚安全开始时间 / Safe by**：考虑耗时、缓冲和更高优先级任务后，最晚应该开始的时间；它不是截止时间。
- **截止时间**：任务必须完成的最终时间。

为减少歧义，中文界面应优先使用“计划开始”和“最晚开始”，不直接裸露英文 “Safe by”。

### 7.3 并行专注

- 每项并行任务拥有自己的开始时间、预计总时长、剩余秒数和实际专注秒数。
- 全局暂停会暂停全部任务。
- 添加新任务时，新任务从添加的时刻开始计时，不追溯此前已经经过的时间。
- 单独完成只删除当前专注会话中的该任务，不结束其他任务。
- 所有任务都完成或用户选择结束全部后，关闭专注页面。

## 8. 技术结构

```text
apps/
  api/                 FastAPI、确定性调度、适配逻辑、可选 Bedrock Agent
  ios/
    Kairos/            SwiftUI 主 App
    KairosWidget/      WidgetKit 扩展
    KairosTests/       规划器、Agent 与专注计时测试
  web/                 Next.js 演示界面与本地 fallback
```

iOS 主要模块：

- `ContentView.swift`：首页、导航、提醒呈现、任务操作与功能编排。
- `FocusView.swift`：单任务/多任务专注、真实时间结算和独立完成。
- `Planner.swift`：排序、计划时间、顺延与 Latest Safe Start。
- `TaskEditor.swift`：任务编辑与习惯页面。
- `NotificationManager.swift`：本地通知安排和清理。
- `CalendarManager.swift`：Apple Calendar 权限和事件写入。
- `AskKairosView.swift`：Agent 交互和确认界面。
- `KairosAdvisor.swift`：本地意图理解和建议生成。
- `WidgetSnapshotStore.swift`：主 App 与 Widget 的共享快照。
- `KairosWidget.swift`：WidgetKit 展示。

## 9. 权限与隐私

- 定位：用于显示本地天气。
- 通知：用于计划开始时间和最晚安全开始时间提醒。
- 日历：仅在用户开启保存选项并完成专注后写入专注事件。
- 本地网络：开发时连接 Mac 上的 Kairos Agent 服务。
- 个人 OpenAI API Key：保存在 iOS Keychain，不存入 SwiftData 或普通日志。
- 任务、习惯和回顾数据当前主要保存在设备本地。

## 10. 验证状态

截至 2026-09-15：

- iPhone 17 Pro Max 模拟器构建成功。
- iOS 自动化测试套件通过。
- 已增加对专注真实时间扣减的测试：后台经过时间会正确减少剩余秒数，且不会减成负数。
- 当天顺延已有测试覆盖：只在同一天内移动，并只和相邻下一项交换。
- 计划开始时间、已完成任务过滤、风险等级与 Agent 响应解析已有测试覆盖。
- 当前仍存在一个与 `WeatherManager` 的 Swift 6 actor isolation 有关的编译警告；当前语言模式下不阻塞构建，但应在升级严格 Swift 6 模式前处理。

## 11. 建议的真机验收清单

1. 创建两项不同长度的任务，并都安排在今天。
2. 启动第一项，选择“普通专注”。
3. 切到其他 App 或锁屏 1–2 分钟，再返回 Kairos，确认时间扣除了真实经过时长。
4. 启动第一项后添加第二项，确认两个倒计时同时变化。
5. 先完成较短任务，确认较长任务继续计时。
6. 开启“保存完成的专注时段”，确认两项任务分别写入 Apple Calendar。
7. 选择“保持当前屏幕”，切出 Kairos，确认该模式暂停并在返回时询问是否继续。
8. 到任务计划开始时间时，分别验证锁屏通知和打开 App 后的全屏提醒。
9. 使用超长中文标题，验证竖屏和横屏均最多两行并显示省略号。
10. 左滑任务，确认红色删除区只在滑动时出现，并验证删除确认。
11. 顺延第一项任务，确认它只与当天的第二项交换。
12. 添加小号和长条 Widget，确认倒数日、截止日期和下一任务正确刷新。

## 12. 后续建议

### 优先级 P0

- 在真机验证普通专注锁屏 5 分钟以上后的时间结算。
- 验证并行专注中分别完成、日历写入和活动回顾的一致性。
- 处理通知权限被拒绝、日历权限被拒绝和 App 被系统终止后的恢复体验。

### 优先级 P1

- 持久化正在进行的专注会话，使 App 被系统终止或手机重启后能够恢复。
- 为并行专注增加单项暂停/继续，而不仅是全局暂停。
- 为倒计时结束增加声音、触觉反馈及用户可选强度。
- 统一全部中英文界面文案，优先提供完整中文体验。
- 修复 `WeatherManager` 的 Swift 6 actor isolation 警告。

### 优先级 P2

- 增加 iCloud/CloudKit 同步。
- 增加 Live Activity 与锁屏实时进度（需要单独设计和系统能力评估）。
- 通过 TestFlight 建立朋友测试流程和反馈收集。
- 增加专注与习惯趋势、周回顾和任务耗时偏差分析。

## 13. 文档维护规则

以后每次完成产品改动时，建议在本文件的时间线中追加：

- 日期与时区。
- 用户问题或产品动机。
- 实际完成的行为变化。
- 修改的主要模块。
- 验证方式与结果。
- 已知限制或未完成事项。

产品构想只有在代码落地并验证后，才应从“后续建议”移动到“已实现功能”。
